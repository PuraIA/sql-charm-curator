import type { ConverterGuideTranslation } from '../converter-guides';

/** 简体中文翻译 /json/to-xml、/xml/to-json、/json/to-yaml、/xml/to-yaml。 */
export const CONVERTER_TRANSLATIONS_ZH: Partial<Record<string, ConverterGuideTranslation>> = {
    'json/to-xml': {
        h1: 'JSON 转 XML 工具',
        tagline: '用 @ 表示属性，用重复的兄弟元素表示数组——映射关系是明确规定的，不是猜出来的。',
        intro: [
            'JSON 和 XML 并不共享同一个数据模型，因此任何在它们之间转换的工具，实际上都是在采用一种约定，并寄望它符合你的需求。这个转换工具的约定很简单明确：以 @ 开头的键变成属性，键名为 #text 的变成文本内容，数组变成该键标签名下重复的兄弟元素——而不是用一个带编号子元素的包裹元素，那是另一种常见的选择，但在实践中可读性更差。',
            '下面的示例已加载到上方的转换工具中。两个订单都带有 @id 属性，会变成两个兄弟元素 <order>，而不是一个 <orders><order>...</order><order>...</order></orders> 的包裹结构——数组键本身（order）已经就是那个重复出现的标签名。',
        ],
        mapping: [
            {
                heading: '@键 变成一个属性',
                body: [
                    '以 @ 开头的键会去掉这个前缀，并按照它在对象中出现的顺序，作为属性附加到对应的元素上。订单对象上的 @id: "1001" 会变成 <order> 标签上的 id="1001"。',
                ],
            },
            {
                heading: '#text 变成文本内容——可以和属性一起使用',
                body: [
                    '一个普通的 JSON 对象没有地方可以表达"这个元素既有属性又有文本"——对象没有像 XML 元素那样隐含的文本位置。#text 就是为此设置的显式键：{"@id": "1", "#text": "hello"} 会生成 <tag id="1">hello</tag>。',
                ],
            },
            {
                heading: '数组变成重复的元素，而不是一个包裹元素',
                body: [
                    'order: [ {...}, {...} ] 会生成两个兄弟元素 <order>——键本身就提供了重复出现的标签名。一个没有这样的键的裸数组（直接转换一个 JSON 数组，没有任何东西包裹它）没有天然可复用的标签名，因此会退回到在一个默认的 <root> 内为每个条目使用通用的 <item>。',
                ],
            },
            {
                heading: 'null 变成一个空元素；其他任何标量都变成文本',
                body: [
                    'JSON 的 null 在 XML 中没有对应形式，因此会变成一个自闭合的空元素：null -> <key/>。数字和布尔值会变成它们的字符串形式作为元素文本——8080 会变成文本 "8080"——因为 XML 中的文本永远只是字符。',
                ],
            },
        ],
        limitations: [
            '不是合法 XML 名称的 JSON 键——包含空格、以数字开头、大多数标点符号——会被重写而不是被拒绝：无效字符会变成 _，仍然以数字开头的名称会在前面加上一个 _。这是一种可见的、经过测试的转换（2fa 会变成 _2fa），不是静默进行的，但这确实意味着输出的标签名不总是与输入的键完全相同。',
            '当数组中混合了对象和普通值时，把一个 JSON 数组从 XML 转回来再转过去，并不能完全保持稳定——这个映射是围绕形态一致的数组设计的，这已经覆盖了绝大多数真实的 API 响应和配置数组。',
        ],
        faq: [
            {
                q: '为什么用 @ 表示属性，而不是其他约定？',
                a: '这里没有一个标准——好几个 JSON-XML 库都使用 @，它的优点是能与普通键在排序上明显区分开，并且在纯文本的 JSON 键中不会产生歧义。这个转换工具记录了它确切的选择，而不是假设这是唯一合理的方式。',
            },
            {
                q: '我能直接转换一个没有任何东西包裹的 JSON 数组吗？',
                a: '可以——它会被包裹在一个默认的 <root> 中，每个条目变成一个 <item> 元素，因为裸数组没有自己的键可以拿来复用作为重复的标签名。',
            },
            {
                q: '这个转换是可逆的吗？',
                a: '对于这个约定所针对的形态——对象、属性、形态一致的数组——是的：用"XML 转 JSON"把结果转换回去会得到完全相同的 JSON，这一点已经由本站自己的测试验证过。文本和元素混合的内容是唯一的例外：具体原因请参见 /xml/to-json 自身的限制说明。',
            },
            {
                q: '我的 JSON 会被上传到什么地方吗？',
                a: '不会。转换过程在你的浏览器中运行；不会有任何内容被发送到服务器。',
            },
        ],
    },

    'xml/to-json': {
        h1: 'XML 转 JSON 工具',
        tagline: '把属性、重复的标签和文本内容，映射为简单的 JSON 键。',
        intro: [
            '把 XML 转换成 JSON 的难点不在于语法，而在于 XML 携带着一些 JSON 没有原生位置存放的信息：属性，以及与子元素并存而非唯一内容的文本。这个转换工具对这两者的处理规则都是明确规定的，而非隐含推断的——见下方的映射说明——而且这正是 /json/to-xml 反向使用的同一条规则，因此在这两个页面之间来回转换，对于这里所涵盖的形态是稳定的。',
            '下面的示例——一个小型产品数据源，类似某个较旧的内部 API 可能返回的内容——已加载到上方的转换工具中。每个 <product> 都带有一个 sku 属性和一个嵌套的 <price>，而 <price> 本身又带有 currency 属性和文本：这正是需要同时用到 @ 和 #text 才能忠实表示的情况。',
        ],
        mapping: [
            {
                heading: '属性变成带 @ 前缀的键',
                body: [
                    '<product> 元素上的 sku="SKU-100" 会在其 JSON 对象中变成 "@sku": "SKU-100"。当你阅读 JSON 时，@ 让属性在视觉上与子元素明显区分开来，这也正是 /json/to-xml 在反向转换时所寻找的标记。',
                ],
            },
            {
                heading: '只有文本的元素会折叠成一个普通字符串',
                body: [
                    '<name>Wireless Mouse</name> 会直接变成 "name": "Wireless Mouse"——而不是 {"#text": "Wireless Mouse"}——因为这个元素上没有其他任何内容（没有属性、没有子元素），也就不需要 #text 键来与之并存。',
                ],
            },
            {
                heading: '……但一旦同时存在属性，#text 就会出现',
                body: [
                    '上面的 <price> 元素既有属性又有文本，因此无法折叠成一个裸字符串——否则就没有地方存放货币单位了。它会变成 {"@currency": "BRL", "#text": "129.90"}。',
                ],
            },
            {
                heading: '重复的标签会按文档顺序变成一个数组',
                body: [
                    '<products> 下的两个 <product> 元素会变成一个含有两个条目的 "product" 数组，顺序与它们出现的顺序一致——而不是变成两个独立的键，也不会被合并成一个对象。单独一个 <product>（没有兄弟元素）仍然保持为一个普通对象，而不是一个单元素数组。',
                ],
            },
            {
                heading: '每个值都会变成字符串——这是有意为之的',
                body: [
                    'XML 中的文本永远只是字符；XML 本身没有数字或布尔类型。上面的 129.90 会保持为字符串 "129.90"，而不会被解析成数字 129.9，因为那样也会悄悄地把末尾的零给规范化掉。猜测类型正是那样的行为——一种猜测——本站的 JSON 转 TypeScript 工具出于同样的原因，也做出了同样的选择。',
                ],
            },
        ],
        limitations: [
            '混合内容——文本与子元素交错出现，例如 <p>Hello <b>world</b>!</p>——会丢失文本和元素之间的顺序关系：它会变成 {"#text": "Hello !", "b": "world"}，这无法区分它与 "!<b>world</b>Hello " 的不同。这个转换工具是为结构化的、配置和 API 形态的 XML 而构建的，不是为类似散文的标记语言而构建的，而这一点正是它显现出来的地方。',
            'XML 命名空间被当作普通的字符串前缀处理——soap:Envelope 会变成 JSON 键 "soap:Envelope"，作为字面文本，不会根据其 xmlns 声明进行解析。这是一个真实的、有意为之的范围限制：具备命名空间感知的解析是一个比本转换工具所针对的配置和 API 响应型 XML 大得多的问题。',
            '注释和处理指令会被丢弃——它们不是数据，因此没有对应的 JSON 键可以让它们变成。',
        ],
        faq: [
            {
                q: '为什么一个元素变成了普通字符串，另一个却变成了对象？',
                a: '一个没有属性、没有子元素的元素会折叠成仅仅是它的文本，作为一个字符串。带有属性、子元素，或两者兼有的元素则会变成一个对象——因为一个普通字符串没有地方可以附加这些额外信息。',
            },
            {
                q: 'XML 注释会怎样处理？',
                a: '它们会被丢弃。注释是为人类读者记录 XML 用的；它们不属于数据的一部分，因此没有对应的 JSON 值可以让它们变成。',
            },
            {
                q: '它能处理 CDATA 区段吗？',
                a: '能——<![CDATA[ ... ]]> 内部的内容会被逐字当作文本处理，不会被重新解析为标记，就像普通的文本节点一样。',
            },
            {
                q: '我的 XML 会被上传到什么地方吗？',
                a: '不会。解析和转换都在你的浏览器中运行，使用的是本站自己的 XML 解析器，而不是一次服务器调用。',
            },
        ],
    },

    'json/to-yaml': {
        h1: 'JSON 转 YAML 工具',
        tagline: '不需要设计任何约定——YAML 本来就是 JSON 的数据模型，只是写法不同。',
        intro: [
            '与 JSON 转 XML 不同，这个方向不需要发明任何约定：一个 YAML 映射就是一个 JSON 对象，一个 YAML 序列就是一个 JSON 数组，而 YAML 的标量就是 JSON 已经拥有的那些字符串、数字、布尔值和 null。转换实际上只是把相同的值重新序列化一遍——这也是为什么这是本站唯一一个没有关于"信息丢失"的"已知限制"的转换工具。',
            '下面的示例是一个 Kubernetes Deployment 片段——这类文档正是 YAML 被持续使用、而 JSON 几乎从不被用于的场景，这通常也是有人想要这个转换的真正原因：以工具所期望的格式手动编辑结构化数据。',
        ],
        mapping: [
            {
                heading: '嵌套变成缩进',
                body: [
                    'JSON 对象的键会变成 YAML 的块状映射语法（键: 值，缩进在其父级之下），JSON 数组会变成一个块状序列（- 条目，每行一个）。这里不存在需要围绕设计的属性/文本区分，因为这两种格式都没有属性的概念。',
                ],
            },
            {
                heading: '只有在 YAML 会误读时，字符串才会被加上引号',
                body: [
                    'apiVersion: apps/v1 被裸写——一个不加引号的普通标量——因为 YAML 完全能把它当作字符串来解析，没有任何问题。一个看起来像 YAML 数字、布尔值或 null 的值（比如文本 "true"、"null" 或 "123"）会被加上引号，这样它转回来时仍是字符串，而不会被重新解释为那种其他类型。包含 ": "（冒号加空格）的字符串出于同样的原因也会被加上引号：如果不加引号，YAML 会把它读成另一对键值对，而不是一个单独的值。',
                ],
            },
            {
                heading: '多行字符串会变成块状字面量，而不是转义后的单行',
                body: [
                    '一个包含 \\n 的 JSON 字符串会使用 YAML 的 | 块状字面量风格来书写——文本按自己的缩进行排列——而不是写成带有字面反斜杠 n 的单行加引号字符串。它读起来就像原始文本实际呈现的样子，这对于任何类似多行描述，或嵌入在 CI 配置中的 shell 命令来说都很重要。',
                ],
            },
        ],
        limitations: [
            '没有专属于这个方向的限制：每个 JSON 值（对象、数组、字符串、数字、布尔值、null）都有一个直接对应的 YAML 形式，因此这里没有像 XML 那一侧的属性或重复标签名那样需要猜测的内容。唯一需要知道的一点是关于 YAML 本身的，而不是关于这个转换工具的：一个 YAML 文档可以表达 JSON 无法表达的东西（用于重复结构的锚点和别名、一个文件中的多个文档、注释）——把 JSON 转换成 YAML 永远不会产生这些内容，因为 JSON 里根本没有可以生成它们的来源。',
        ],
        faq: [
            {
                q: '这个转换会有信息丢失吗？',
                a: '对于 JSON 本身能表示的任何内容，都不会。每个对象、数组、字符串、数字、布尔值和 null 都会直接映射到它对应的 YAML 形式，没有任何需要猜测的地方。',
            },
            {
                q: '为什么有些字符串被加了引号，有些却没有？',
                a: '只有当不加引号会改变字符串在 YAML 中的含义时，它才会被加上引号——例如字面文本 "true" 或 "123"，如果不加引号，它们会被重新解析成布尔值或数字，而不是字符串。',
            },
            {
                q: '我能用这种方式转换 Kubernetes 清单文件或 docker-compose 文件吗？',
                a: '可以，对于粘贴 JSON、输出 YAML 这个方向来说——大多数基础设施工具两者都能接受，而且这个工具能为任何以合法 JSON 开头的内容生成合法的 YAML。',
            },
            {
                q: '我的数据会被上传到什么地方吗？',
                a: '不会。转换在你的浏览器中运行，这一点在这里很重要，因为 Kubernetes 和 CI 配置经常包含内部服务名称。',
            },
        ],
    },

    'xml/to-yaml': {
        h1: 'XML 转 YAML 工具',
        tagline: '先经过与"XML 转 JSON"相同的 @ / #text 约定，然后再序列化为 YAML。',
        intro: [
            '这实际上是接连执行的两次转换，而不是一次独立的直接转换：XML 会被解析成与 /xml/to-json 相同的、带有 @ 属性 / #text / 重复标签约定的 JSON 表示形式，然后这个值会被写成 YAML 而不是 JSON。/xml/to-json 上记录的每一条规则和每一项限制在这里都同样适用——这个页面之所以存在，是因为"xml 转 yaml"是有人真正会搜索的关键词，而不是因为底层的转换有任何不同。',
            '这个示例复用了与 /xml/to-json 相同的产品数据源，因此你可以直接对比两种输出：相同的 @sku 和 #text 键，只是写成了 YAML 映射而不是 JSON 对象。',
        ],
        mapping: [
            {
                heading: '属性和文本完全遵循 XML 转 JSON 的约定',
                body: [
                    '下面的 @sku: SKU-100 和 #text: \'129.90\' 与 /xml/to-json 上记录的那些带 @ 前缀的属性键和 #text 内容键完全相同，只是渲染成了 YAML 的 键: 值 语法，而不是 JSON 的 "键": "值"。YAML 一侧的加引号规则遵循 YAML 通常的规则："@sku" 因为以 @ 开头的键需要引号而被加上引号，"129.90" 被加上引号是为了让它保持为字符串，而不是被重新读取为数字。',
                ],
            },
            {
                heading: '重复的元素会变成一个 YAML 序列',
                body: [
                    '两个兄弟元素 <product> 会变成一个 product: 键，其中包含一个 YAML 序列（- @sku: ... / - @sku: ...）——数组这一步发生在 XML 解析期间，与 /xml/to-json 完全相同；只有最后的序列化步骤不同。',
                ],
            },
            {
                heading: '来自 XML 的数字在 YAML 中仍然是加引号的字符串',
                body: [
                    '上面的 129.90 会以加引号的 YAML 字符串 \'129.90\' 形式出现，而不是裸数字 129.9——因为它一开始就从来不是一个数字。XML 中的文本永远都是字符（具体原因请参见 /xml/to-json 的映射说明，了解为什么这个转换工具不会做相反的猜测），因此 YAML 这一步得到的是一个需要序列化的字符串，它会像对待任何看起来像数字的字符串一样，用同样的方式给它加上引号。',
                ],
            },
        ],
        limitations: [
            '/xml/to-json 的每一项限制在这里都会首先适用，甚至在 YAML 介入之前：混合内容会丢失顺序，命名空间被当作字面字符串前缀处理，注释会被丢弃，每个 XML 文本值都会变成字符串，而不会被解析成数字或布尔值。',
        ],
        faq: [
            {
                q: '这与 XML 转 JSON 是不同的转换吗？',
                a: '不是——它使用完全相同的规则解析 XML，然后把结果序列化为 YAML 而不是 JSON。每一条映射规则和限制都与 /xml/to-json 共用。',
            },
            {
                q: '为什么 YAML 输出中有些键会被加上引号，比如 \'@sku\'？',
                a: 'YAML 要求给原本会产生歧义的普通标量加上引号——以 @ 开头的键就是这样一种情况。这是 YAML 的语法要求，不是这个转换工具额外添加的东西。',
            },
            {
                q: '我的 XML 会被上传到什么地方吗？',
                a: '不会。解析步骤和 YAML 序列化都在你的浏览器中运行。',
            },
        ],
    },
};
