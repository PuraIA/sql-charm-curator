import type { DialectSlug, DialectGuideTranslation } from '../sql-dialects';

/** 简体中文翻译 /sql/<dialect>。模式参见 sql-dialects.pt.ts。 */
export const DIALECT_TRANSLATIONS_ZH: Partial<Record<DialectSlug, DialectGuideTranslation>> = {
    postgresql: {
        h1: 'PostgreSQL 格式化工具',
        tagline: '类型转换、JSONB 运算符和 LATERAL 连接，格式化时不会被拆散。',
        intro: [
            '格式化 PostgreSQL 代码时，麻烦很少出在 SELECT 列表上。真正的难点在于 Postgres 积累了一系列看起来像普通标点符号的运算符：:: 用于类型转换，-> 和 ->> 用于 JSON 访问，@> 表示包含，?| 表示键存在性检测。天真地按词法切分的格式化工具最终会把这些运算符从中间切断，或者把开头的冒号误认为命名参数标记，从而在错误的位置断行。',
            '本页面使用选定的 PostgreSQL 语法运行格式化工具，参数语法设置为命名参数（:name）加编号参数（$1），这正是 Postgres 线路协议和大多数驱动程序所使用的方式。下面的示例已加载到上方的编辑器中，你可以切换选项，看同一条查询如何重新格式化。',
        ],
        quirks: [
            {
                heading: ':: 类型转换运算符与 :命名参数 的冲突',
                body: [
                    'PostgreSQL 用 :: 表示类型转换，用 : 引入命名参数，这让同一个字符承担了两种含义。如果格式化工具按照 : 只是参数标记的方言配置，它会把 o.total::numeric 开头的冒号读成占位符的起始，从而拆散这个表达式。',
                    '选择 PostgreSQL 语法后，解析器会知道 :: 作为单一的类型转换运算符绑定，而 :name 和 $1 仍然是占位符。类型转换会紧贴其表达式，就像上面那一行 (c.profile -> \'prefs\' ->> \'locale\')::TEXT 一样。',
                ],
            },
            {
                heading: 'JSONB 运算符，包括 ? 系列',
                body: [
                    'Postgres 通过运算符而非函数来暴露 JSON 访问：-> 返回 jsonb，->> 返回文本，#> 和 #>> 接受路径数组，@> 测试包含关系。它们被当作运算符处理，因此只要表达式宽度允许，操作数就会保持在同一行。',
                    '存在性检测运算符值得特别说明。? 测试顶层键，?| 测试数组中的任意键，?& 测试所有键——而 ? 同时也是 JDBC 及其他多个驱动使用的位置占位符。在选定 PostgreSQL 语法的情况下，它们被解释为运算符：',
                ],
            },
            {
                heading: '美元符引用的函数体保持原样',
                body: [
                    '用 $$ 或 $tag$ 括起来的 PL/pgSQL 函数体，对 SQL 解析器而言就是一个很长的字符串字面量。格式化工具会精确保留它，而不是重新缩进——这是安全的做法，因为重新格式化字符串内部会改变存储在目录中的实际值。',
                    '实际效果是：CREATE FUNCTION 语句中周围的 SQL 会被格式化，而函数体本身保持单行不变。如果你想格式化函数体本身，请把它当作独立代码块单独格式化，然后再粘贴回去。',
                ],
            },
            {
                heading: 'FILTER、LATERAL 及其他可嵌套的子句',
                body: [
                    'COUNT(*) FILTER (WHERE ...) 在聚合调用内部放置了一整个 WHERE 子句，而 LEFT JOIN LATERAL 在连接内部放置了一整个子查询。两者都被展开为嵌套代码块，这让查询结构一目了然——你能立刻看出这个 LATERAL 子查询是"每行取前 1 条"的查找模式。',
                    '带 RETURNING 子句的 ON CONFLICT ... DO UPDATE 也是同样处理，包括对 excluded 伪表的引用。',
                ],
            },
            {
                heading: '标识符大小写折叠：为什么 Preserve 是正确的默认值',
                body: [
                    'PostgreSQL 会把未加引号的标识符折叠为小写，因此 MyTable 和 mytable 是同一个对象，而用双引号括起来的 "MyTable" 则是不同的对象。这使得标识符的大小写在大多数其他方言中并不重要，但在这里却至关重要。',
                    '因此标识符大小写选项默认设置为 Preserve。改成 Upper 或 Lower 会连带重写带引号的标识符，可能导致查询指向一个根本不存在的表。关键字大小写是独立的设置，所以你仍然可以把 SELECT 和 FROM 大写，而不影响你的架构名称。',
                ],
            },
        ],
        limitations: [
            'DISTINCT ON (col) 会被放到 SELECT DISTINCT 之后单独另起一行，而不是紧挨着它。结果仍是合法的 SQL，但可读性不如其余部分。',
            '在美元符引用的函数体内部，格式化工具不做任何改动，包括 CREATE FUNCTION 语句末尾的 language plpgsql 部分，它会保持你输入时的大小写。',
        ],
        conventions: [
            {
                heading: '关键字大写，标识符原样保留',
                body: [
                    '最常见的 Postgres 风格是把保留字大写，同时保留表名和列名原有的 snake_case 写法（即架构创建时的样式）。这正是这里的默认配置：关键字、数据类型和函数的大小写设为 Upper，标识符大小写设为 Preserve。',
                ],
            },
            {
                heading: '优先使用 CTE，但要知道它们不再是优化屏障',
                body: [
                    '把查询拆分成 WITH 代码块比嵌套三层子查询更易读，而且从 PostgreSQL 12 开始，简单的 CTE 会被查询计划器内联而不是物化，所以可读性不再以牺牲执行计划为代价。如果确实需要旧版的屏障行为，请显式加上 MATERIALIZED。',
                    '"语句间空行"选项控制格式化工具在各语句之间插入的空行，这能让包含多条语句的迁移脚本保持可读。',
                ],
            },
        ],
        faq: [
            {
                q: '格式化会改变 PostgreSQL 执行查询的方式吗？',
                a: '不会。解析器在规划之前就已经去除了空白并折叠了未加引号的标识符，因此格式化后的查询与写成一行的同一查询，执行计划完全相同。格式化只是为了方便阅读代码的人。',
            },
            {
                q: '这会破坏我用引号括起来的"驼峰式"标识符吗？',
                a: '在默认设置下不会。标识符大小写设置为 Preserve，因此加引号的标识符会原样保留，与你写的完全一致。只有在你确定整个架构都使用未加引号、大小写不敏感的名称时，才应该更改这个设置。',
            },
            {
                q: '它能格式化 PL/pgSQL 函数体吗？',
                a: '从 SQL 解析器的角度看，$$ 分隔符之间的函数体是一个字符串字面量，因此会原样保留而不会重新缩进。要格式化函数体本身，请只粘贴分隔符之间的那部分代码。',
            },
            {
                q: '我的查询会被发送到什么地方吗？',
                a: '不会。格式化工具是一个在你浏览器中运行的 JavaScript 库。没有任何内容会被上传，因此你可以放心粘贴包含生产环境表名和列名的查询。',
            },
        ],
    },

    mysql: {
        h1: 'MySQL 和 MariaDB 格式化工具',
        tagline: '反引号标识符、索引提示，以及原样保留的双参数 LIMIT。',
        intro: [
            '格式化 MySQL 时反复出现的问题是标识符。保留字列表每个版本都在增长——rank、groups、window 和 system 在 8.0 中都变成了保留字——因此真实的架构中充斥着用反引号括起来的列名，仅仅是因为该名称与某个关键字冲突。如果格式化工具把反引号当作装饰性符号处理，或者对其中的文本套用关键字大小写规则，就会生成无法正常工作的 SQL。',
            '选择 MySQL 语法可以保持反引号引用不变，并正确解析该方言特有的子句：位于表和连接之间的索引提示、双参数的 LIMIT，以及形如子句的 GROUP_CONCAT 参数。下面的查询已加载到上方编辑器中。',
        ],
        quirks: [
            {
                heading: '反引号标识符与大小写敏感性',
                body: [
                    '示例中名为 `order` 的列是常见情况：一个完全合理的业务术语，恰好也是保留字。反引号是让这条查询保持合法的唯一手段，因此它们会被保留，绝不会被重新调整大小写。',
                    '这一点在 MySQL 中比在大多数数据库中更重要，因为表名的大小写敏感性取决于宿主的文件系统。在 Linux 上，Users 和 users 是不同的表；在 macOS 和 Windows 上通常不是。把标识符大写的格式化工具在开发环境中会悄无声息地正常工作，到生产环境却会报错，这就是为什么标识符大小写在这里默认设置为 Preserve。',
                ],
            },
            {
                heading: '双参数 LIMIT',
                body: [
                    'MySQL 同时支持 LIMIT 数量 和 LIMIT 起始位置, 数量 两种写法。双参数形式在标准 SQL 中没有对应写法——LIMIT 40, 20 表示跳过 40 条，返回 20 条，这与人们对 LIMIT ... OFFSET 的直觉顺序恰好相反。',
                    '格式化工具会把两个参数保持在同一行，而不是在逗号处断开，因为拆开只会让本已令人困惑的子句变得更难理解。',
                ],
            },
            {
                heading: '索引提示位于表和连接之间',
                body: [
                    'FORCE INDEX、USE INDEX、IGNORE INDEX 和 STRAIGHT_JOIN 附着在表引用上，因此它们出现在别名之后、下一个 JOIN 之前。它们被解析为表引用的一部分，并保留在同一行，这让连接列表保持可读。',
                ],
            },
            {
                heading: '参数本身包含子句的函数',
                body: [
                    'GROUP_CONCAT 不是普通的函数调用：它的参数列表可以包含 DISTINCT、一整个 ORDER BY 以及 SEPARATOR。因此它被展开为嵌套代码块，ORDER BY 缩进在调用内部。',
                ],
            },
            {
                heading: 'ON DUPLICATE KEY UPDATE，新旧两种写法',
                body: [
                    '这个 upsert 子句在两种写法中都能被识别——传统的 VALUES(col) 形式，以及 MySQL 8.0.20 引入的、使其被弃用的行别名形式。带别名的写法会被规范地解析：',
                ],
            },
            {
                heading: '三种注释语法，其中一个陷阱',
                body: [
                    'MySQL 支持行末的 #、块状的 /* */，以及行末的 --。后者有一个令人意外的前提条件：MySQL 要求双短横线后必须跟一个空格，因此 --comment 并不是注释，通常会被解析为减法运算后跟一个标识符。',
                    '注释会原样保留在原位。如果一个 -- 注释在格式化后依然存在，但查询在执行时却报错，请检查是否缺少那个空格。',
                ],
            },
        ],
        limitations: [
            '在 DATE_SUB(NOW(), interval 30 day) 中，INTERVAL 关键字及其单位会保留你输入时的大小写，因为它们被解析为函数参数的一部分，而不是顶层关键字。两种写法在语法上都是合法的。',
        ],
        conventions: [
            {
                heading: '只对需要的部分加引号',
                body: [
                    '给每个标识符都加反引号是从无条件生成反引号的可视化工具那里继承来的习惯。这样做并不算错，但会增加噪音——上面的示例给 `users` 和 `order` 加了反引号，别名则不加，这是手写代码中最常见的风格。',
                ],
            },
            {
                heading: 'MariaDB 使用相同的设置',
                body: [
                    'MariaDB 在 5.5 版本之后与 MySQL 出现了分歧，但与格式化相关的语法——反引号、索引提示、LIMIT、注释风格——是共通的。对 MariaDB 查询请使用 MySQL 方言选项。',
                ],
            },
        ],
        faq: [
            {
                q: '把关键字大写会破坏我大小写敏感的表名吗？',
                a: '不会。关键字大小写和标识符大小写是相互独立的设置。默认设置会把 SELECT、FROM 和 JOIN 大写，同时让每个表名和列名保持你输入时的原样。',
            },
            {
                q: '这适用于 MariaDB 吗？',
                a: '适用。选择 MySQL 方言即可。影响格式化的语法在两者之间是相同的。',
            },
            {
                q: '为什么我的 -- 注释没有被当作注释处理？',
                a: 'MySQL 要求双短横线后必须有一个空格字符。--note 会被解析为一个表达式；-- note 才是注释。这是 MySQL 本身的规则，不是格式化工具的行为。',
            },
            {
                q: '我的 SQL 会被发送到服务器吗？',
                a: '不会。格式化过程发生在你的浏览器中，任何内容都不会离开你的电脑，因此你可以放心粘贴包含真实架构名称的查询。',
            },
        ],
    },

    't-sql': {
        h1: 'SQL Server T-SQL 格式化工具',
        tagline: '方括号标识符、APPLY 运算符、窗口函数和 GO 批处理分隔符。',
        intro: [
            'T-SQL 包含的过程式语法比其他任何主流方言都多，而且其中相当一部分根本不算真正的 SQL——GO 是一个客户端指令，表变量用与参数标记相同的 @ 符号声明，查询提示则以括号形式跟在语句末尾。正确格式化 T-SQL 主要在于分清哪些是语句、哪些只是装饰。',
            '下面加载到上方编辑器中的查询涵盖了最容易出问题的部分：用方括号括起的多部分名称、与外层行相关联的 OUTER APPLY，以及带有自己 PARTITION BY 和 ORDER BY 的窗口化 ROW_NUMBER。',
        ],
        quirks: [
            {
                heading: '方括号标识符与四部分命名',
                body: [
                    'SQL Server 用方括号引用标识符，一个完全限定名最多可以有四个部分：server.database.schema.object。方括号会被保留，其中的点不会被当作运算符处理，因此 [dbo].[Customers] 保持原样不变。',
                    '当 QUOTED_IDENTIFIER 设置为 ON（大多数驱动的默认设置）时，双引号也可以作为标识符分隔符。两种写法都能被识别。',
                ],
            },
            {
                heading: 'CROSS APPLY 和 OUTER APPLY',
                body: [
                    'APPLY 是 T-SQL 的横向连接：右侧的子查询会针对左侧表的每一行求值一次，并且可以引用该行的列。CROSS APPLY 会剔除不产生任何结果的外层行，OUTER APPLY 则会用 NULL 保留它们——这与 INNER JOIN 和 LEFT JOIN 的关系相同。',
                    '子查询会被展开为嵌套代码块，让关联关系一目了然。在示例中，APPLY 内部的 o.[CustomerID] = c.[CustomerID] 正是把它与外层行连接起来的部分。',
                ],
            },
            {
                heading: '窗口函数',
                body: [
                    '一个 OVER 子句可以包含 PARTITION BY、ORDER BY 和窗口帧规范，使其成为选择项内部的嵌套子句。每一部分都单独占一行，而不是挤在一起，因为读错 PARTITION BY 是最容易得到一个看似合理却错误答案的方式之一。',
                    '像 ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW 这样的窗口帧子句会与其关键字保持在同一行。',
                ],
            },
            {
                heading: 'GO 是批处理分隔符，不是语句',
                body: [
                    'GO 只被 SSMS、Azure Data Studio 和 sqlcmd 识别，SQL Server 引擎本身并不认识它——客户端在此处切分脚本，并分别发送每个批处理。包含 GO 的脚本会被正确处理：各个批处理独立格式化，分隔符单独保留一行。',
                ],
            },
            {
                heading: 'MERGE',
                body: [
                    'MERGE 使用一个源，针对一个目标同时执行插入、更新和删除操作，分支包括 WHEN MATCHED、WHEN NOT MATCHED BY TARGET 和 WHEN NOT MATCHED BY SOURCE。每个分支都从新的一行开始，让三种情况分别清晰可读：',
                ],
            },
            {
                heading: 'WITH 前面的分号',
                body: [
                    '脚本经常用 ;WITH 而不是 WITH 来开始一个 CTE。这个分号用于终止前一条语句，因为 WITH 本身是有歧义的——它同时也用于引入表提示——而 SQL Server 要求在 CTE 之前的语句必须已经结束。',
                    '这个习惯本身无害，格式化工具也能接受它，但给每条语句都加上分号结尾会让这个习惯变得多余，这也是更好的做法。',
                ],
            },
        ],
        limitations: [
            'TOP (n) WITH TIES 无法被正确解析：WITH 会被读作公用表表达式的开始，导致该子句被拆成多行，变成 TOP (n) / WITH / ties。因此本页的示例使用不带 WITH TIES 的 TOP (3)。如果你需要 WITH TIES，请先格式化查询，然后手动修正这一个子句。',
            '在 MERGE 语句中，USING 关键字会保留你输入时的大小写，而不会像其他关键字一样被大写化。',
        ],
        conventions: [
            {
                heading: 'PascalCase 架构命名，标识符大小写设为 Preserve',
                body: [
                    'SQL Server 架构惯例上使用 PascalCase——CustomerID、OrderDate——而 SQL Server 依据数据库的排序规则比较标识符，通常是大小写不敏感的。Preserve 仍是正确的默认值：它能保持你的命名可读，也能避免在少数使用大小写敏感排序规则的数据库上出意外。',
                ],
            },
            {
                heading: '为语句加上结束符',
                body: [
                    '多个版本以来，Microsoft 一直把省略语句结束符标记为已弃用。给每条语句都加上分号结尾，就不再需要开头分号这个技巧，也能让格式化工具可靠地在各语句之间插入空行。',
                ],
            },
        ],
        faq: [
            {
                q: '我可以粘贴带 GO 分隔符的脚本吗？',
                a: '可以。各批处理会被独立格式化，每个 GO 都保留在自己的一行。GO 是客户端指令而非 T-SQL 语句，因此会原样传递而不会被当作语句解析。',
            },
            {
                q: '为什么 TOP (3) WITH TIES 会被拆散？',
                a: '解析器把 WITH 读作公用表表达式的开始。这是上面列出的一个已知限制。查询的其余部分会正常格式化，因此通常的解决方法是格式化之后再手动修正这一个子句。',
            },
            {
                q: '它能格式化存储过程内部的代码吗？',
                a: '过程内部的语句会作为普通 T-SQL 被格式化。IF 和 WHILE 之类的流程控制结构能被识别，但结果不如简单的 SELECT 那样整洁——过程式代码是所有 SQL 格式化工具的薄弱环节。',
            },
            {
                q: '我的 T-SQL 会被发送到服务器吗？',
                a: '不会。所有处理都在你的浏览器中进行，因此包含内部架构名称或对象名称的查询永远不会离开你的电脑。',
            },
        ],
    },

    'oracle-plsql': {
        h1: 'Oracle SQL 和 PL/SQL 格式化工具',
        tagline: '优化器提示原样保留，层级查询与 (+) 传统连接语法。',
        intro: [
            'Oracle 是格式化真正可能改变行为的方言，原因在于一个特性：优化器提示是以注释的形式书写的。如果格式化工具规范化或去除了注释，就会悄悄删掉 /*+ INDEX(...) */，改变一个看起来依然完全正确的查询的执行计划。精确保留提示是处理任何 Oracle SQL 的首要前提。',
            '除了提示之外，Oracle 还承载着数十年积累下来的语法——早于 ANSI 连接的 (+) 外连接记号、用于层级结构的 CONNECT BY、DUAL，以及处理含单引号字符串的 q-quote 机制。下面加载到上方编辑器中的查询同时用到了好几种。',
        ],
        quirks: [
            {
                heading: '优化器提示是有实际意义的注释',
                body: [
                    '提示写作 /*+ ... */，必须紧跟在 SELECT、INSERT、UPDATE、DELETE 或 MERGE 关键字之后出现。放在其他位置，Oracle 会悄无声息地忽略它——这正是为什么会移动注释位置的格式化工具在这里是危险的。',
                    '提示会被保留在原位置，其内容永远不会被重新调整大小写或重新格式化。在上面的输出中，/*+ index(e emp_dept_ix) */ 仍然紧跟在 SELECT 之后，并保留了书写时的小写形式。',
                ],
            },
            {
                heading: '传统 (+) 外连接',
                body: [
                    '在 ANSI 连接语法被支持之前，Oracle 用连接谓词上的 (+) 标记外连接的可选一侧。这在旧代码中仍然非常常见。d.department_id(+) 表示 departments 表的那一行可能不存在——等同于从 employees 出发的 LEFT JOIN。',
                    '这种记号会被保留。注意标记前会插入一个空格，生成 d.department_id (+)，Oracle 对此的解析结果完全相同。',
                ],
            },
            {
                heading: '层级查询：CONNECT BY、LEVEL、ORDER SIBLINGS BY',
                body: [
                    'Oracle 使用 START WITH 选择根节点，使用 CONNECT BY PRIOR 描述父子关系，来遍历树形结构，并通过 LEVEL 伪列暴露深度。ORDER SIBLINGS BY 则在每一层内部排序，同时不破坏层级结构。',
                    '这些都是顶层子句，因此它们与 WHERE 和 GROUP BY 保持相同的缩进层级，而不是折叠进它们所跟随的 WHERE 子句中。',
                ],
            },
            {
                heading: 'q-quote 字符串字面量',
                body: [
                    '在字符串中把每个单引号都写两遍很容易出错，因此 Oracle 提供了另一种引用方式：q\'[...]\'——或者任意一对分隔符——让内容按字面处理。分隔符和内容都会被精确保留：',
                ],
            },
            {
                heading: 'DUAL、NVL 和 DECODE',
                body: [
                    'DUAL 是 Oracle 的单行表，每当表达式需要一个 FROM 子句时都会用到它。NVL 是双参数的空值替换函数，DECODE 则是早于 CASE 的位置式条件表达式。三者都被当作普通标识符和函数处理。',
                    '对于新代码，COALESCE 和 CASE 是 NVL 和 DECODE 的可移植等价物，只是在类型转换和短路求值方面略有不同。',
                ],
            },
        ],
        limitations: [
            'PL/SQL 代码块的格式化远不如查询精细。DECLARE / BEGIN / END 代码块会被拆成各段之间带空行的独立行，而不是像嵌套结构那样缩进。代码块内部的 SQL 语句会正常格式化；代码块本身的框架结构则不会。',
            '在 FETCH FIRST 25 rows ONLY 中，rows 一词会保持小写，因为它被解析为行数限制子句的一部分，而不是独立关键字。',
            'SQL*Plus 用于执行代码块的结尾斜杠是客户端指令，不属于 PL/SQL。粘贴代码时请不要包含它。',
        ],
        conventions: [
            {
                heading: '新代码优先使用 ANSI 连接',
                body: [
                    '(+) 记号无法表达 full outer join，不能与同一查询中的 ANSI 连接语法混用，而且很难把连接条件和过滤条件区分开来。LEFT JOIN 更清晰，Oracle 多年来也一直如此推荐。格式化旧的 (+) 代码有助于阅读；转换它则是另一项独立的工作。',
                ],
            },
            {
                heading: 'ROWNUM 与 FETCH FIRST 的对比',
                body: [
                    '当与 ORDER BY 结合使用时，用 ROWNUM 限制行数需要一个内联视图，因为 ROWNUM 是在排序之前分配的——这是"取前 N 条"查询返回错误 N 行的经典成因。Oracle 12c 引入了 FETCH FIRST n ROWS ONLY，它会先排序，这也正是上面示例所使用的写法。',
                ],
            },
        ],
        faq: [
            {
                q: '优化器提示会被保留吗？',
                a: '会。提示会保留在紧跟起始关键字之后的位置，其内容不会被修改。这正是上面示例所展示的行为。',
            },
            {
                q: '它能格式化包体或大型 PL/SQL 代码块吗？',
                a: '内部的 SQL 语句会被格式化，但代码块的框架结构处理得不够好——这一点已列在已知限制中。对于过程式代码，使用专门针对 PL/SQL 的 IDE 格式化工具效果会更好。',
            },
            {
                q: '我需要保留末尾的斜杠吗？',
                a: '不需要。/ 是 SQL*Plus 用来执行前面代码块的指令，它不属于 PL/SQL 本身。粘贴语句时请不要包含它。',
            },
            {
                q: '我的 SQL 会被上传到什么地方吗？',
                a: '不会。格式化完全在你的浏览器中进行——这一点在这里尤为重要，因为 Oracle 查询经常嵌入架构名称和业务逻辑。',
            },
        ],
    },

    bigquery: {
        h1: 'BigQuery SQL 格式化工具',
        tagline: '反引号限定名、UNNEST、SELECT * EXCEPT 和 QUALIFY。',
        intro: [
            'BigQuery 查询由其他方言所没有的两样东西塑造：用单一反引号括起、内部以点号分隔的三部分表名，以及本质上是结构体数组而非标量的列。这两点都改变了格式化工具需要正确处理的内容——`project.dataset.table` 内部的点号不是运算符，而 FROM 子句中的 UNNEST 虽然看起来像函数调用，实际上是一种连接。',
            'GoogleSQL 还新增了一些能消除整层嵌套的子句。QUALIFY 可以基于窗口函数进行过滤，而不需要标准写法所要求的外层子查询；SELECT * EXCEPT 则可以剔除某些列，而不必列出你要保留的那些。下面加载到上方编辑器中的查询两者都用到了。',
        ],
        quirks: [
            {
                heading: '反引号括起的限定名',
                body: [
                    'BigQuery 的表引用写作 `project.dataset.table`，点号位于引号内部，而不是分别引用各个部分。项目标识符中常常包含短横线——示例中的 analytics-prod——这正是为什么反引号是必需的：没有它们，短横线会被解析成减法运算。',
                    '整个引用被当作单一的标识符词法单元处理，因此永远不会在点号或短横线处被切断。',
                ],
            },
            {
                heading: 'UNNEST 是一种连接，不是函数调用',
                body: [
                    '当某一列是 ARRAY 类型时，FROM 子句中的 UNNEST 会把它展平成多行，并与其来源的那一行相关联。前面的逗号相当于 CROSS JOIN，这就是为什么示例写作 FROM table e, UNNEST(e.hits) AS h——每个 hit 一行，同时携带所属的会话信息。',
                    '它在 FROM 列表中独占一行，与它所展开的表处于同一缩进层级，这如实反映了它的本质。',
                ],
            },
            {
                heading: 'SELECT * EXCEPT 和 * REPLACE',
                body: [
                    '宽事件表使得逐列列出变得不切实际，因此 GoogleSQL 允许改为减法式的写法：* EXCEPT (payload) 选择除该列以外的所有列，而 * REPLACE (expr AS col) 在保留其余部分的同时替换某一列的值。两者都被解析为星号的修饰符，而不是函数调用。',
                ],
            },
            {
                heading: 'QUALIFY',
                body: [
                    '基于窗口函数进行过滤通常需要在子查询中先计算，再在外层进行过滤，因为 WHERE 是在窗口函数之前执行的。QUALIFY 让这一切在单一层级内完成——示例在没有外层 SELECT 的情况下，保留了每个用户最近的一个事件。',
                    '这是一个顶层子句，与 WHERE 和 GROUP BY 并列放置。QUALIFY 要求同一查询块中存在 WHERE、GROUP BY 或 HAVING，或者一个 WINDOW 子句。',
                ],
            },
            {
                heading: '通配符表和 _TABLE_SUFFIX',
                body: [
                    '表名末尾的 * 会匹配所有共享该前缀的表，_TABLE_SUFFIX 伪列则包含匹配到的那部分——这是遍历按日期分区的导出数据的标准方式。对 _TABLE_SUFFIX 进行过滤正是阻止查询扫描每一个分片的关键，因此它属于 WHERE 子句，而不是后续的过滤步骤。',
                    'SAFE_CAST 以及 SAFE. 函数前缀会在输入无效时返回 NULL，而不是抛出错误——当数据是用户提供的 JSON 时，这一点很重要。两者都被识别为普通的函数语法。',
                ],
            },
        ],
        limitations: [
            '_table_suffix、_partitiontime 和 _partitiondate 这些伪列会保留你输入时的大小写，因为它们被解析为标识符而不是关键字。BigQuery 对它们的大小写不做要求。',
            'DECLARE、SET、BEGIN ... END、EXECUTE IMMEDIATE 之类的脚本语句，格式化效果远不如查询语句精细——这与所有方言中的过程式代码情况相同。',
        ],
        conventions: [
            {
                heading: '格式化不会改变查询的费用',
                body: [
                    'BigQuery 按扫描的字节数计费，这取决于所引用的列和涉及的分区——与空白字符无关。格式化查询永远不会改变它的费用。SELECT * 会改变费用，这正是当表很宽时，应该优先使用 EXCEPT 而不是星号的真正理由。',
                ],
            },
            {
                heading: '仅支持 GoogleSQL',
                body: [
                    'Legacy SQL——2016 年之前使用方括号写法 [project:dataset.table] 的旧方言——是另一套不同的语法，本工具不支持。如果你的查询在表名中使用冒号和方括号，那就是 Legacy SQL，需要迁移而不是格式化。',
                ],
            },
        ],
        faq: [
            {
                q: '格式化会影响我的查询费用吗？',
                a: '不会。费用取决于扫描的字节数——即所引用的列和读取的分区。空白字符和关键字大小写对两者都没有影响。',
            },
            {
                q: '它支持 Legacy SQL 吗？',
                a: '不支持，只支持 GoogleSQL（曾称为 Standard SQL）。Legacy SQL 使用不同的表引用语法和不同的语法体系。',
            },
            {
                q: 'STRUCT 和 ARRAY 表达式会被处理吗？',
                a: '会。嵌套的 STRUCT 构造函数和 ARRAY_AGG 调用会被解析为普通表达式，UNNEST 也会被识别为 FROM 子句的一部分。',
            },
            {
                q: '我的查询会被发送给 Google 或某个服务器吗？',
                a: '不会。本页面在你的浏览器中运行格式化工具。查询不会被发送到任何地方，甚至也不会发送给 BigQuery。',
            },
        ],
    },
};
