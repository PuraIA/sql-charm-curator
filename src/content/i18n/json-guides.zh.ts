import type { JsonGuideSlug, JsonGuideTranslation } from '../json-guides';

/** 简体中文翻译 /json/<slug>。可用字段参见 json-guides.ts。 */
export const JSON_TRANSLATIONS_ZH: Partial<Record<JsonGuideSlug, JsonGuideTranslation>> = {
    minify: {
        h1: 'JSON 压缩工具',
        tagline: '去掉每一个只为人眼存在的字节，并与 JSON.stringify 的结果核对一致。',
        intro: [
            '压缩 JSON 是本站有意做得最简单的转换：工具在已经解析好的值上调用不带缩进参数的 JSON.stringify()，因此空白消失了，其余一切都不变——相同的键、相同的键顺序、相同的数字、相同的字符串内容。这里没有一个可能出错的独立压缩算法，因为引擎自身的序列化器就是压缩工具本身。',
            '这种简单性也正是这个页面存在的原因：压缩很容易做对，也很容易被误判为出错。下面的示例已经加载到上方编辑器中，并选中了"压缩"，因此你可以粘贴自己的 JSON，观察完全相同的替换在其上发生。',
        ],
        sections: [
            {
                heading: '实际被移除的内容',
                body: [
                    '只有存在于结构性标记之间的空白——{ 之后、} 之前、: 或 , 前后——才是 JSON.stringify() 在没有缩进参数时从不输出的空白。这不是对文件的文本扫描；它和语言中其他任何 JSON.stringify() 调用走的是同一条代码路径，因此不会有手写字符串压缩工具可能引入的那些转义错误。',
                    '键的顺序会与解析对象中的顺序完全一致地保留。JavaScript 对象对字符串键保持插入顺序（有一个例外——像 "1" 或 "42" 这样类似整数的键，无论它们在源中出现的位置如何，总是会被优先按数字排序，排在其他所有键之前）。这种重新排序是 JavaScript 规范的一部分，不是这个工具额外添加的。',
                ],
            },
            {
                heading: '"压缩"（Minified）与"紧凑"（Compact）不是一回事',
                body: [
                    '上方的格式选择器还提供"紧凑"选项，这是一种不同的、更温和的转换：JSON.stringify(parsed, null, 1)——用一个空格缩进代替两个或四个，但仍然每行一个值。压缩会完全去除结构；紧凑只是把它缩小。当仍需要有人阅读差异对比时使用紧凑，当结果只会被机器消费时使用压缩。',
                ],
            },
            {
                heading: '压缩真正节省字节的场景',
                body: [
                    '如果响应已经以 gzip 或 brotli 压缩方式提供，那么压缩带来的大部分收益在到达网络之前就已经消失了：重复的空白正是这些算法本来就能很好消除的那种冗余。压缩最重要的场景是未被压缩的负载——某些 webhook 请求体、本地缓存、内嵌的配置文件——以及减少 JSON.parse() 在处理非常大的文档时需要做的工作，因为无论压缩与否，更少的字节都意味着需要扫描更少的字符。',
                ],
            },
        ],
        limitations: [
            '压缩不会改变数字的格式、不会移除未使用的字段，也不会缩短键名——它只移除空白。如果你需要在此之外获得更小的负载，那是一次架构层面的改动，而不是压缩设置能解决的。',
            '对于数兆字节的文档，JSON.parse() 和 JSON.stringify() 都会在主线程上同步运行。粘贴非常大的内容时，标签页可能会在处理期间短暂无响应；这是浏览器 JSON 实现本身的特性，不是这个页面在其之上额外添加的。',
        ],
        faq: [
            {
                q: '压缩会以任何方式改变数据吗？',
                a: '不会。每个键、值和数组元素都会被精确保留。只有它们之间的空白会被移除。',
            },
            {
                q: '"压缩"和"紧凑"有什么区别？',
                a: '压缩会移除所有空白，生成单独一行。紧凑保持每行一个值，但缩进最小。当仍需要有人阅读时使用紧凑，当只有机器会读取时使用压缩。',
            },
            {
                q: '如果我的 API 响应已经是 gzip 压缩的，这还有帮助吗？',
                a: '帮助比你预期的要小。Gzip 已经能高效压缩重复的空白，因此在压缩之前先做 JSON 压缩带来的额外节省，会比上面显示的字节数所暗示的要小。对于未压缩的负载，它更重要。',
            },
            {
                q: '我的 JSON 会被上传到什么地方吗？',
                a: '不会。JSON.parse() 和 JSON.stringify() 都在你的浏览器中运行。没有任何内容会被发送到服务器，这在负载中包含真实客户或账户数据时尤为重要。',
            },
        ],
    },

    validate: {
        h1: 'JSON 校验工具',
        tagline: '借助解析器自身的错误信息，准确看到你的 JSON 违反了哪条规则。',
        intro: [
            '这个工具校验 JSON 的方式，与你浏览器中每一次 JSON.parse() 调用完全相同，因为它底层运行的正是这个函数——不存在一个更宽松的独立校验层。如果 JSON.parse() 接受你的输入，工具就会报告它有效；如果它抛出异常，工具会在"JSON 原始内容"标签旁显示该异常的消息，并附带绿色的 ✓ 或红色的 ⚠ 标记。',
            '下面的四个示例都是真实的输入，以及基于 V8 的引擎（Chrome、Edge 和 Node.js 都使用 V8）针对每一个输入所抛出的真实错误文本，这些内容是通过真正把它们传给 JSON.parse() 生成的，而不是凭记忆描述的。Firefox 和 Safari 使用不同的 JavaScript 引擎，对这些错误的措辞也不同，但它们出于相同的根本原因拒绝相同的输入。',
        ],
        invalidExamples: [
            {
                label: '多余的逗号',
                explanation:
                    'JSON 中没有"多余逗号"这个概念。与 JavaScript 对象字面量不同，闭合 } 之前的逗号后面必须始终跟着另一个 "键": 值 对。',
            },
            {
                label: '未加引号的键',
                explanation:
                    '每个对象键都必须是用双引号括起来的字符串。这在 JavaScript 对象字面量中是合法的，这也是为什么当 JSON 是手写而非自动生成时，这个错误很常见的原因。',
            },
            {
                label: '// 注释',
                explanation:
                    'JSON 完全没有注释语法——既没有 //，也没有 /* */。有些工具接受 "JSONC"（带注释的 JSON）作为输入格式，但标准的 JSON.parse() 不接受。',
            },
            {
                label: '前导零',
                explanation:
                    'JSON 数字在其他数字之前不能有前导零（如 01、007）。这与 JavaScript 数字字面量的同一条规则相呼应，其存在是为了避免与八进制表示法产生歧义。',
            },
        ],
        sections: [
            {
                heading: '"有效"意味着什么，又不意味着什么',
                body: [
                    '这个工具检查你的文本是否是符合 RFC 8259 的格式良好的 JSON——每个花括号都匹配，每个字符串都加了引号，每个值都是正确的形态。这与"这是不是我的应用程序期望的 JSON"是一个不同且更狭窄的问题。一个缺少必需字段的响应，或者在你的代码期望数字的地方发送了字符串的响应，仍然是完全有效的 JSON，会通过这项检查，同时依然会破坏你的应用程序。',
                    '要捕捉第二类问题需要架构校验——一个 JSON Schema 文档，或者像 Zod 这样的运行时类型检查器——针对你期望的具体形态进行核对。这个工具是应该放在那一步之前的第一道快速检查，而不是它的替代品。',
                ],
            },
            {
                heading: '为什么错误信息是具体的',
                body: [
                    '一个只说"JSON 无效"的校验器会迫使你用肉眼扫视整个文档。这个工具显示的信息——与 JSON.parse() 抛出的信息相同——包含字符位置，在大多数引擎中还包含行号和列号，这通常足以让你直接跳到错误位置，而无需手动与已知正确的副本进行对比。',
                ],
            },
        ],
        limitations: [
            '错误信息的确切措辞是基于 V8 的引擎特有的。Firefox 和 Safari 会以不同的措辞报告相同的违规情况，因此如果你在排查某个使用这些浏览器之一的用户提交的报告，预期消息文本——而不是底层问题本身——会有所不同。',
            '这里只检查语法。有效但形态错误的 JSON（缺少字段、用字符串代替数字）不会在这里被标记出来；这需要针对你自己期望的结构进行架构校验。',
        ],
        faq: [
            {
                q: '为什么它只说 JSON 无效，却没有更多细节？',
                a: '并非如此——切换到"格式化 JSON"标签（或者直接开始输入），就会显示解析器的确切错误，包括字符位置。上面的示例正是这条消息本身，并已根据 JSON.parse() 的真实输出进行了核实。',
            },
            {
                q: '"有效"是否意味着我的 API 会接受它？',
                a: '这意味着 JSON 格式良好。具体的字段和类型是否与某个 API 所期望的相符，是一个这个工具不回答的独立问题——那需要针对该 API 的约定进行架构校验。',
            },
            {
                q: '我能校验包含注释的 JSON（JSONC）吗？',
                a: '用这个工具不行——它是针对没有注释语法的标准 JSON（RFC 8259）进行检查的。如果你处理的是 JSONC 配置文件，请先去掉注释。',
            },
            {
                q: '我的 JSON 会被发送到某处进行检查吗？',
                a: '不会。校验是通过浏览器自身的 JSON.parse() 在本地运行的。没有任何内容会被上传。',
            },
        ],
    },

    'to-typescript': {
        h1: 'JSON 转 TypeScript 工具',
        tagline: '把真实的 API 响应转换成带名称的接口——包括可选字段。',
        intro: [
            '粘贴一个 API 响应并得到带类型的接口，实际上是与格式化 JSON 完全不同的一项工作，因此它拥有自己独立的生成器，而不是又一种附加在美化打印工具上的输出样式。它对解析后的值遍历一次：每个对象都变成一个带名称的接口，对象数组会被合并成一个单一接口，而在数组的部分（但非全部）条目中缺失的字段会变成可选，而不是为每个条目生成一个单独的类型。',
            '下面的示例已加载到上方编辑器中，并选中了 JSON → TypeScript。这是一个真实的形态——一个带有嵌套地址的用户记录、一个字符串标签数组，以及一个订单数组，其中只有一个订单带有 trackingCode——之所以选择这个例子，是因为它涵盖了三个重要的情况：嵌套、数组和不一致的可选字段。',
        ],
        sections: [
            {
                heading: '嵌套如何变成带名称的接口',
                body: [
                    '每个对象字段都会获得自己的接口，以该字段命名（address 变成 Address，orders 的元素变成 Order）。这是刻意设计的：内联的嵌套类型比带名称的类型更难以复用，在编辑器悬停提示中也更难阅读。两个键和类型集合完全相同的字段——例如账单地址和收货地址——会被识别为同一种形态，并共享一个接口，而不是生成重复的接口。',
                ],
            },
            {
                heading: '对象数组会被合并，而不是逐一列举',
                body: [
                    'orders 是一个数组，其中第一个条目没有 trackingCode，第二个有。生成器不会生成 Order 和 Order2，而是查看数组中的每一个条目，收集出现在其中任意一个条目里的所有键的并集，并在某个键至少在一个条目中缺失时将其标记为可选——这正是上面 trackingCode?: string 所表达的意思。这与你在真正阅读了几个示例响应之后，自己手动定义类型的方式是一致的。',
                ],
            },
            {
                heading: '混合类型的数组会变成联合类型',
                body: [
                    '一个元素类型并非全部相同的数组——[1, "two", 3]——会生成 (number | string)[]，而不是选择一种类型来掩盖不一致，也不是拒绝生成任何内容。空数组没有可供推断的内容，会被标注为 unknown[]；一旦你知道这个数组应该存放什么内容，就可以手动收窄它的类型。',
                ],
            },
            {
                heading: '它刻意不去推断的内容',
                body: [
                    '每个字符串都会变成 string，每个数字都会变成 number——它不会尝试检测某个字段总是看起来像日期、电子邮件，或者三个固定值之一，并将其收窄为字面量联合类型或带标记的类型。可靠的类型收窄要么需要比单个响应更大的样本量，要么需要这个工具所不具备的领域知识；猜错会比把字段保留为 string 更糟糕。',
                ],
            },
        ],
        limitations: [
            '一个示例响应只是一个样本，而不是一份架构定义。某个字段在你粘贴的这唯一一份响应中恰好是 null 或整数，但在其他响应中有时是字符串或小数，这样的字段会被类型标注得过于狭窄。请用几个不同的真实响应来测试生成的接口，而不仅仅是一个。',
            '生成的接口名和属性名来自你的 JSON 键，除了完全相同的字段形态之外不会被去重——两个来自名为 "data" 的字段、但形态不同的对象，都会被命名为 Data 和 Data2，读起来像是"第二个"而不是任何有描述性的名称。在你了解它们各自代表什么之后，请重新命名它们。',
        ],
        faq: [
            {
                q: '它能处理深度嵌套的 JSON 吗？',
                a: '可以——嵌套没有深度限制。无论出现在多少层深度，每个嵌套对象都会变成它自己的带名称接口。',
            },
            {
                q: '如果一个数组混合了对象和原始值会怎样？',
                a: '对象条目会像往常一样合并为一个接口，原始值条目贡献它们自己的类型，而数组的元素类型是两者的并集——例如 (Order | string)[]。',
            },
            {
                q: '我能设置自己的根接口名称吗？',
                a: '当你在工具中编辑 JSON 时，生成的根接口默认命名为 Root。本页面之所以将其命名为示例所代表的内容（ApiUser），纯粹是为了让示例更易读。',
            },
            {
                q: '我的 JSON 会被发送到服务器来生成类型吗？',
                a: '不会。生成器在你的浏览器中运行，从不上传你粘贴的内容——这一点很重要，因为你在这里粘贴的很可能正是一个真实的 API 响应。',
            },
        ],
    },
};
